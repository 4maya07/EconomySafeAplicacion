import 'package:flutter/material.dart';

/// Vista placeholder para el panel principal hasta construir el dashboard real.
class PrincipalVista extends StatelessWidget {
  const PrincipalVista({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dashboard en construcción'),
      ),
    );
  }
}
